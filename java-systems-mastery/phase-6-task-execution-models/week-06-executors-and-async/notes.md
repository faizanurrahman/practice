## Week 06 - Executors and Async (Senior/Staff Interview Starter Stub)

This week focuses on the coordination levers in production:

- thread pools and queueing models (latency vs throughput)
- `ExecutorService` basics and rejection policies
- `Future` and `CompletableFuture` composition semantics (exception propagation)
- work-stealing intuition (ForkJoinPool at a conceptual level)
- cancellation and structured shutdown thinking

Artifacts will come later in `theory/`, `examples/`, `concepts/`, and `practice/`.

