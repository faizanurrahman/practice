## Week 05 - Threads and Locks (Senior/Staff Interview Starter Stub)

This week builds the concurrency foundation interviewers expect:

- Thread lifecycle and execution model (run queue, scheduling, interruption)
- `synchronized` (monitorenter/monitorexit) and lock states
- `volatile` memory semantics (visibility + ordering)
- fairness, contention, and common deadlock/livelock patterns

Add artifacts later:
- `theory/`: JMM happens-before mental model, lock ordering, false sharing
- `examples/`: small lock/volatile scenarios
- `concepts/`: minimal lock primitive (illustrative)
- `practice/`: deadlock diagnosis exercise

