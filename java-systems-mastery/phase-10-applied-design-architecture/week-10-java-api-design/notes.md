## Week 10 - Applied API and System Design (Lead/Staff Stub)

This week connects runtime understanding to real engineering decisions:

- designing stable public APIs (contracts, evolution, compatibility)
- module boundaries and encapsulation (JPMS mental model)
- dependency direction and layering
- error strategy across layers (wrapping, retries, idempotency)
- concurrency model selection in a service/library
- observability: what to emit (logs/metrics/traces) and where

Later we will add artifacts (theory/examples/concepts/practice) for concrete design tasks.

