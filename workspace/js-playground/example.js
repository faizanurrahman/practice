/**
 * Phase 0 — tiny JS example. Run: node example.js
 * Break it (e.g. change to let x after console.log) and log why in ../debug-log.md
 */

const x = 42;
console.log("x =", x);

function greet(name) {
  return `Hello, ${name}`;
}
console.log(greet("World"));
