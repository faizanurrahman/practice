# 11 — Modules practice

Run from the `javascript` directory:

```bash
node practice/11-modules/main.js
```

Demonstrates: named exports (`math.js`), default export (`logger.js`), and dynamic `import()`.
